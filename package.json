{
  "name": "deploy-nextjs-example-app",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "test": "standard",
    "lint": "standard",
    "dev": "nodemon --quiet --exec 'standard && echo linting: OK' --watch '**/*.js' --ignore node_modules"
  },
  "bin": {
    "deploy-nextjs-example-app": "./bin"
  },
  "dependencies": {
    "find-project-files": "^0.0.4",
    "shelljs": "^0.8.3",
    "ulid": "^2.3.0"
  },
  "devDependencies": {
    "standard": "^12.0.1"
  }
}
